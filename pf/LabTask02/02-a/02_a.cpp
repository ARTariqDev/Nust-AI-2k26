#include <iostream> // used to provide/include standard library functions (or for processing fundamental input/output)
using namespace std; //used this to make code more concise,
// makes it so that we dont have to specify that the current line uses the standard library each time

int main() { //the main function/entry point
    cout << "CS110 Programming Lab\n"; //cout statement (console output)
    cout << "Student:\t Abdur Rehman Tariq\n"; //semi colons are used to specify the end of a line (for the compiler)
    cout << "Section:\t BS AI-A \n";
    return 0; // since main is type int, returning 0 is makes it a flag to tell the compiler the what the end of the program is
} //closing braces to specify the end of a main function